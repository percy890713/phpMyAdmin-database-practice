# MySQL 新手手冊 — 從零開始

> 從安裝環境到用 Python 操作資料庫的完整入門說明。

---

## 目錄

1. [專案背景](#專案背景)
2. [環境架構](#環境架構)
3. [安裝與設定](#安裝與設定)
4. [Python 連接資料庫](#python-連接資料庫)
5. [常用 SQL 指令速查](#常用-sql-指令速查)

---

## 專案背景

這個專案是為了學習資料庫基礎而建立的，目標是：

- 了解 MySQL 資料庫的基本概念（資料庫、資料表、欄位、列）
- 學會使用 phpMyAdmin 視覺化管理資料庫
- 學會用 SQL 語法進行 CRUD（新增、查詢、更新、刪除）操作
- 學會用 Python 程式連接 MySQL 並操作資料

---

## 環境架構

```
瀏覽器（phpMyAdmin）
        ↓
    XAMPP（本機伺服器）
    ├── Apache（網頁伺服器，Port 80）
    └── MySQL / MariaDB（資料庫，Port 3306）
        └── my_practice_20260505（資料庫）
            └── orders（資料表）

VS Code（Python）
    └── mysql-connector-python
            ↓
    MySQL / MariaDB（Port 3306）
```

### 為什麼用 XAMPP？

XAMPP 是一個「懶人包」，一次安裝好 Apache + MySQL + PHP + phpMyAdmin，省去分別安裝設定的麻煩，適合本機開發與學習使用。

### phpMyAdmin 是什麼？

phpMyAdmin 是一個網頁介面的 MySQL 管理工具，讓你不用打指令就能用視覺化方式管理資料庫。開啟方式：啟動 XAMPP 後在瀏覽器輸入 `localhost/phpmyadmin`。

---

## 安裝與設定

### 1. 安裝 XAMPP

- 下載網址：https://www.apachefriends.org/download.html
- 選擇 Windows 最新版本（8.2.x）
- 安裝路徑建議：`C:\xampp`（不要裝在 Program Files）

### 2. 啟動服務

開啟 XAMPP Control Panel，點擊以下兩個服務的 **Start**：
- **Apache** — 啟動後顯示 Port 80, 443
- **MySQL** — 啟動後顯示 Port 3306

### 3. 進入 phpMyAdmin

瀏覽器輸入：`localhost/phpmyadmin`

預設帳號：
- 使用者名稱：`root`（或自建帳號）
- 密碼：安裝時設定的密碼

### 4. 安裝 Python 套件

```bash
pip install mysql-connector-python
```

---

## Python 連接資料庫

### 基本連線範例

```python
import mysql.connector

conn = mysql.connector.connect(
    host='127.0.0.1',
    port=3306,
    user='TestingProject0505',  # 你的帳號
    password='你的密碼',
    database='my_practice_20260505'
)

cursor = conn.cursor()
```

### 查詢所有訂單

```python
cursor.execute("SELECT * FROM orders")
rows = cursor.fetchall()
for row in rows:
    print(row)
```

### 新增一筆訂單

```python
sql = """
INSERT INTO orders (customer_name, product, quantity, price, order_date, status)
VALUES (%s, %s, %s, %s, %s, %s)
"""
data = ('測試客戶', '滑鼠', 2, 500, '2024-04-01', '待處理')
cursor.execute(sql, data)
conn.commit()
```

### 更新訂單狀態

```python
sql = "UPDATE orders SET status = %s WHERE id = %s"
cursor.execute(sql, ('已完成', 5))
conn.commit()
```

### 刪除訂單

```python
cursor.execute("DELETE FROM orders WHERE id = %s", (10,))
conn.commit()
```

### 關閉連線

```python
cursor.close()
conn.close()
```

> 重要：所有 INSERT / UPDATE / DELETE 操作之後都必須執行 `conn.commit()`，否則資料不會真正寫入資料庫。

---

## 常用 SQL 指令速查

### 查詢

```sql
-- 查詢所有資料
SELECT * FROM orders;

-- 查詢特定欄位
SELECT customer_name, product, price FROM orders;

-- 依條件篩選
SELECT * FROM orders WHERE status = '待處理';

-- 依日期篩選
SELECT * FROM orders WHERE order_date >= '2024-02-01';

-- 排序
SELECT * FROM orders ORDER BY price DESC;

-- 計算總金額
SELECT SUM(price * quantity) AS 總營業額 FROM orders;

-- 計算各狀態訂單數
SELECT status, COUNT(*) AS 數量 FROM orders GROUP BY status;
```

### 新增

```sql
INSERT INTO orders (customer_name, product, quantity, price, order_date, status)
VALUES ('王小明', '滑鼠', 1, 500, '2024-04-01', '待處理');
```

### 更新

```sql
UPDATE orders SET status = '已完成' WHERE id = 5;
```

### 刪除

```sql
DELETE FROM orders WHERE id = 10;
```

---

*建立日期：2026-05-05*
*學習工具：XAMPP 3.3.0 / MariaDB 10.4.21 / Python 3.x / phpMyAdmin 5.1.1*
